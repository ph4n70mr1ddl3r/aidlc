PORT=3000
DB_PATH=./data/itmanager.db
# MUST be changed to a random string >= 32 characters in production
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=CHANGE_ME_TO_A_RANDOM_STRING_AT_LEAST_32_CHARS
# MUST be changed to a random string >= 32 characters in production
CSRF_SECRET=CHANGE_ME_TO_A_RANDOM_STRING_AT_LEAST_32_CHARS_TOO
NODE_ENV=development
# Enable when behind a reverse proxy (accepts '1' or 'true'). Only turns on Express
# trust proxy setting when explicitly enabled to avoid IP spoofing on direct connections.
TRUST_PROXY=0
# Set to auto-prune audit_log entries older than N days on startup (e.g. 365)
# Leave unset or 0 to disable pruning.
PRUNE_AUDIT_DAYS=0
# Interval (ms) between periodic audit log pruning cycles. Default 86400000 (24h).
# Only used when PRUNE_AUDIT_DAYS > 0. Set to 0 to disable periodic pruning.
# PRUNE_AUDIT_INTERVAL_MS=86400000
# Production session store package (e.g. 'connect-sqlite3', 'connect-redis').
# The package must be installed (npm install connect-sqlite3) and a SESSION_STORE=connect-sqlite3
# configured. MemoryStore (the default) is NOT suitable for production.
# SESSION_STORE=connect-sqlite3
# Dashboard aggregation cache TTL (ms). Default 30000 (30s), minimum 1000 (1s),
# maximum 3600000 (1h). Values outside this range are clamped.
# DASHBOARD_TTL_MS=30000
# Default page size for paginated list views (default: 25, max: 100)
# PAGE_SIZE=25
# Seed passwords (only used by npm run seed; change defaults in shared/CI environments)
# If unset, the seeder generates a strong random password per role and prints
# it to the console. Set these to use fixed passwords in a shared dev/CI env.
# SEED_ADMIN_PASSWORD=<strong-admin-password>
# SEED_PASSWORD=<strong-staff-password>
# Set SEED_VERBOSE to '1' or 'true' to print auto-generated seed passwords to the
# console. Default is off (passwords are not printed for security).
# SEED_VERBOSE=0
# OVERRIDE: allow seeding in production (DANGEROUS — drops and re-creates all data).
# Must be set to '1' explicitly; seeding is blocked in production without it.
# SEED_DANGER=0
